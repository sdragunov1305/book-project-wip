Put your EPUB file here and name it exactly:

  main.epub

Then the game will load it on startup (see config/book_source.json).

Alternatively edit book_source.json "epub_path" to an absolute path to your .epub file.
Save that JSON as UTF-8 if the path contains non-ASCII letters.
